using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParcelBox : ItemInfo
{
    // Start is called before the first frame update
    void Start()
    {
       // maxCarryingCapacity = 1;  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
